{
  "name": "donation-page-cypress-research",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --browser electron",
    "cypress:run:headed": "cypress run --headed --browser electron",
    "cypress:run:spec": "cypress run --browser electron --spec cypress/e2e/DonationDetails/CancerResearchDonation.cy.js"
  },
  "devDependencies": {
    "cypress": "^15.20.0"
  }
}
